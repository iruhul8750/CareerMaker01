git checkout dev
git pull origin dev

# fix code

git add .
git commit -m "Fix production bug"
git push origin dev

git checkout main
git pull origin main
git merge dev
git push origin main

# if accidentally merge and want to unmerge and go to previous commit of main (when not pushed main)
#  To see the commit logs
git log --oneline

type "q" to exit

# Go to Previous commit of the active branch
git reset --hard HEAD~1